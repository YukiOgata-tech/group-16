# coding: UTF-8
"""
走行チューニング用パラメータ集

実機の速度やコースに合わせて、このファイルだけを調整します。
"""

# センサー正規化（実機の最大値に合わせて補正）
SENSOR_MIN = 0.0           # センサー最小値（黒）
SENSOR_MAX = 0.45          # センサー最大値（白）: 実機の上限に合わせる

# しきい値（白=1, 黒=0 の前提）
BLACK_THRESHOLD = 0.6       # 黒判定のしきい値
CROSS_BLACK_COUNT = 3       # 交差点とみなす黒センサー数
CROSS_FORWARD = 0.35        # 交差点直進時の速度

# 走行速度系
BASE_SPEED = 0.30           # 基本速度（直進時）
MIN_BASE_SPEED = 0.12       # 最低速度（急カーブで減速してもここまで）
SPEED_SCALE = 1.00          # 全体速度スケール（0.5〜1.2 目安）

# 旋回ゲイン（行列マッピング係数）
OUTER_GAIN = 1.00           # 外側センサーの効き
INNER_GAIN = 0.20           # 内側センサーの効き
LINE_WEIGHTS = (-1.5, -0.5, 0.5, 1.5)  # ライン位置推定の重み

# カーブ減速
TURN_SLOWDOWN = 0.60        # 旋回量に応じて減速する割合 (0-1)

# ラインロスト時の探索
LOST_BLACK_THRESHOLD = 0.15 # これ未満ならラインロスト扱い
LOST_TURN_SPEED = 0.18      # 探索時の旋回速度
LOST_FORWARD_SPEED = 0.00   # 探索時の前進速度

# 出力制限
OUTPUT_CLAMP = 1.00         # モータ指令の最大値
